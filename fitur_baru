menambahkan fitur:

1. variable scope, sehingga perlu mengubah handleVar, generateIdentifier, handleIf, handleFor untuk menyesuaikan dengan scope.
2. tanbah gitur di be untuk handle error
3. function tanpa parameter, karna untuk parameter diubah dlu di parser agar bisa mengembalikan variabel bukan sekedar string nama variabel (bllm buat)